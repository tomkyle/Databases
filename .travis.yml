language: php
php:
  - "5.5"
  - "5.4"

before_script:
  - composer require --dev aura/sql:1.3
  - composer install --dev --no-interaction
  - mysql -e 'create database tomkyle_test;'

script:
  - mkdir -p build/logs
  - phpunit


after_script:
  - php vendor/bin/coveralls -v
